# ADRs

Architecture Decision Records are stored in this folder as one file per decision.

Use filenames in the form:

`YYYY-MM-DD-short-decision-name.md`

Each ADR should describe context, decision, consequences, and any alternatives that were rejected.

